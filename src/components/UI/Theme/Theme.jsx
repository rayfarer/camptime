import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#E59400', // Replace this with your desired shade of orange
        },
    },
    // ... other theme configurations
});

export default theme;